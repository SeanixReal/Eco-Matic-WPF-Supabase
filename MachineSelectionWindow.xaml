<Window x:Class="Eco_Matic.MachineSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Vending Machine"
        Height="420" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="MachineButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="60" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Background" Value="#F2F6FC" />
            <Setter Property="Foreground" Value="#2E77E6" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderBrush" Value="#C5D3EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Margin="20,10,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,0,16,-1" Panel.ZIndex="3">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCloseWindow"
                        Content="x"
                        Style="{StaticResource WindowCloseTab}"
                        Margin="6,0,0,0"
                        Click="BtnCloseWindow_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="#C5D3EB" BorderThickness="1" CornerRadius="16" MouseLeftButtonDown="WindowFrame_MouseLeftButtonDown">
            <Border.Effect>
                <DropShadowEffect Color="#2F0C264A" BlurRadius="25" ShadowDepth="6" />
            </Border.Effect>

            <Border CornerRadius="15" ClipToBounds="True">
                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Select a Kiosk" FontSize="26" FontWeight="Bold" Foreground="#2E77E6" HorizontalAlignment="Center" Margin="0,0,0,4" />
                    <TextBlock Grid.Row="1" x:Name="txtStatus" Text="Please pick a machine location to interact with." Foreground="#6A7891" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <ItemsControl x:Name="icMachines" Grid.Row="2">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" Rows="2" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding DisplayName}" 
                                        Tag="{Binding MachineId}" 
                                        Style="{StaticResource MachineButtonStyle}" 
                                        Click="BtnMachine_Click" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>
